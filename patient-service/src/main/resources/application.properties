spring.application.name=patient-service

# uncomment to use in-memory database h2
# spring.h2.console.path=/h2-console
# spring.datasource.url=jdbc:h2:mem:patientdb;
# spring.datasource.driver-class-name=org.h2.Driver

# spring.datasource.username=admin
# spring.datasource.password=password

# spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# # sync entities to database for auto database updates
# spring.jpa.hibernate.ddl-auto=update
# spring.sql.init.mode=always    

spring.kafka.producer.key-serializer=org.apache.kafka.common.serialization.StringSerializer
spring.kafka.producer.value-serializer=org.apache.kafka.common.serialization.ByteArraySerializer

spring.kafka.bootstrap-servers=kafka:9092  # External Kafka broker address

server.port=4000
logging.level.root=info
 
